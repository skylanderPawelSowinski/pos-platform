{
  "name": "pos-platform",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "test": "turbo test",
    "test:db:setup": "docker exec pos-postgres psql -U postgres -c \"CREATE DATABASE pos_platform_test\" || true && DATABASE_URL=postgresql://postgres:postgres@localhost:5432/pos_platform_test pnpm --filter @repo/database migrate",
    "docker:rundb": "docker compose -f infrastructure/docker/compose.yml up -d",

    "format": "biome format . --write",
    "check": "biome check ."
  },
  "devDependencies": {
    "@biomejs/biome": "^2.2.4",
    "turbo": "^2.5.5",
    "typescript": "^5.9.2"
  }
}
